"""dynamics.recipes -- custom transform kayit yeri.

Bir recipe: fit(train_df, plan_ctx)->self ; transform(X)->X' ; get_params()/serialize.
registry'ye isimle kayitli; AdaptivePlan icinden recipe:"<ad>" ile cagrilir.
"""
