"""engines.runners.base — EngineRunner protokolü: run(engine_spec, data_ref) -> result_ref.

Implementasyonlar: InProcessRunner (varsayilan), SubprocessRunner, ContainerRunner (v2+), RemoteRunner (v2+).
"""
