# Benchmark koşumları için opt-in modelleri aç (ADR 0040/0041).
# Katalogta `enabled: false` olan ağır girişler burada açılır; `run.py` / `forecasting.py`
# bunu `model_catalog_override` ile geçer. (ngboost/tabicl artık üretimde de açık — ADR 0040.)

auto_tbats:
  enabled: true
